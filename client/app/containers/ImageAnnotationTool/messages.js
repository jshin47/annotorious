/*
 * ImageAnnotationTool Messages
 *
 * This contains all the text for the ImageAnnotationTool container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ImageAnnotationTool';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ImageAnnotationTool container!',
  },
});
