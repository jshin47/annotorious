// import { fromJS } from 'immutable';
// import { selectImageAnnotationToolDomain } from '../selectors';

describe('selectImageAnnotationToolDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
