/*
 * SignUpForm Messages
 *
 * This contains all the text for the SignUpForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SignUpForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Sign up',
  },
});
