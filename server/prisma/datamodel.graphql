type User {
    id: ID! @unique

    displayName: String!

    createdAt: DateTime!
    updatedAt: DateTime!
}

type SystemUserLogin {
    id: ID! @unique
    user: User!
    environmentVariable: String! @unique
    systemUsername: String! @unique

    createdAt: DateTime!
    updatedAt: DateTime!
}
